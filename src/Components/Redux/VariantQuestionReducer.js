const VARIANT_TITLE = 'VARIANT_TITLE';

let defaultState = {
    variantAnswer:{

    }
};

const variantAnswerReducer = (state=defaultState,action) => {
    switch (action.type) {
        case VARIANT_TITLE:






    }
};








export const addVariantOneTitle = (variantTitleFlag) => ({type:VARIANT_TITLE, variantTitleFlag});

export default variantAnswerReducer;